import React from "react";

export const DefaultMessageBox = () => {
    return (
        <div className="min-h-screen w-full flex justify-center items-center text-[rgba(255,255,255,0.5)]">
            No conversation select for see messages
        </div>
    );
};
